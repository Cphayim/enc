import './style.css'

export enum EncCSSVariables {
  // colors
  BackgroundColor = '--enc-background-color',
  PrimaryColor = '--enc-primary-color',

  // transition / animation
  TransitionRate = '--enc-transition-rate',
  TransitionDuration = '--enc-transition-duration',
  TransitionTimingFunction = '--enc-transition-timing-function',
  AnimationRate = '--enc-animation-rate',
  AnimationDuration = '--enc-animation-duration',
  AnimationTimingFunction = '--enc-animation-timing-function',

  // safe area
  SafeAreaInsetTop = '--enc-safe-area-inset-top',
  SafeAreaInsetBottom = '--enc-safe-area-inset-bottom',
  SafeAreaInsetLeft = '--enc-safe-area-inset-left',
  SafeAreaInsetRight = '--enc-safe-area-inset-right',

  // scrollbar
  ScrollbarSize = '--enc-scrollbar-size',
  ScrollbarColor = '--enc-scrollbar-color',
  ScrollbarHoverColor = '--enc-scrollbar-hover-color',
  ScrollbarInactiveColor = '--enc-scrollbar-inactive-color',
  ScrollbarBorderWidth = '--enc-scrollbar-border-width',
  ScrollbarBorderStyle = '--enc-scrollbar-border-style',
  ScrollbarBorderColor = '--enc-scrollbar-border-color',
  ScrollbarBorderRadius = '--enc-scrollbar-border-radius',
  ScrollbarTrackColor = '--enc-scrollbar-track-color',
}
